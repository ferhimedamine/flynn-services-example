# github.com/lmars/flynn-services-example

A simple Go web app which, when run in a Flynn cluster, reports how many other example services it can see via discoverd.

It refreshes the page every two seconds.
